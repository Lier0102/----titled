#include <stdio.h>
#include <string.h>

int dp[2501][2501];
char str[2501];

int main(void) {
    scanf("%s", str);

    int l = strlen(dp);

    for (int i = 0; i < l; i++) {

    }

    printf("%d\n", dp[l]); // 최솟값은 배열의 마지막에 저장.

    return 0;
}

//YdhY?K5SYaF8@sH